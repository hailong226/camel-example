<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0"
           xmlns:ext="http://aries.apache.org/blueprint/xmlns/blueprint-ext/v1.0.0"
           xsi:schemaLocation="
       http://www.osgi.org/xmlns/blueprint/v1.0.0 http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd
       http://camel.apache.org/schema/blueprint http://camel.apache.org/schema/blueprint/camel-blueprint.xsd">

    <cm:property-placeholder id="example" persistent-id="org.github.example"
                             placeholder-prefix="{{" placeholder-suffix="}}" update-strategy="reload">
        <cm:default-properties>
            <!--<cm:property name="say" value="Hi from Camel" />-->
        </cm:default-properties>
    </cm:property-placeholder>

    <ext:property-placeholder>
        <ext:location>file:src/test/resources/org.github.example.cfg</ext:location>
    </ext:property-placeholder>

    <bean id="helloBean" class="org.github.HelloBean">
        <property name="say" value="${say}"/>
    </bean>

    <camelContext xmlns="http://camel.apache.org/schema/blueprint" useBlueprintPropertyResolver="true">
        <route id="timerToLog">
            <from uri="timer:foo?period=5000"/>
            <setBody>
                <method ref="helloBean" method="hello"/>
            </setBody>
            <log message="The message contains ${body}"/>
            <to uri="mock:result"/>
        </route>
    </camelContext>

</blueprint>
